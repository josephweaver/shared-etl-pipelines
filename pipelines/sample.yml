project_id: land_core
vars:
  message: "Hello Midwest"
  name: sample
dirs:
  workdir: "{env.workdir}/{name}/{sys.now.yymmdd}/{sys.now.hhmmss}-{sys.run.short_id}"
  logdir: "{workdir}/logs"
steps:
  - name: echo
    plugin: echo
    args:
      message: "{message}"
    when: "True"
