project_id: land_core
shared_with_projects:
  - gee_lee

requires_pipelines:
  - pipelines/yanroy/build.yml

steps:
  - name: yanroy_subfolder_entrypoint
    script: 'echo.py message="YanRoy root wrapper complete; use pipelines/yanroy/*.yml for modular jobs."'
